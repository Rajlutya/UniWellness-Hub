<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Support Request History</title>
    <link rel="stylesheet" href="/CSS/style.css">
    <style>
        /* Additional styles specific to pasthistory page */
        .container {
            max-width: 800px;
            margin: 80px auto;
        }
        .container h1 {
            text-align: center;
            margin-bottom: 20px; 
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            border: 2px solid #ccc; /* Border around the table */
        }
        th, td {
            border: 1px solid #ccc;
            padding: 8px;
            text-align: left;
        }
        th {
            text-align: center;
            background-color: #ffffff;
        }
        tbody tr:nth-child(even) {
            background-color: #D7E8D8; /* Lighter shade of green */
        }
        tbody tr:nth-child(odd) {
            background-color: #ECEFF1; /* Main background color */
        }
    </style>
</head>
<body>
    {{> newnav}}
    <div class="container">
        <h1>Past History</h1>
        <table>
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Time</th>
                    <th>Issue</th>
                    <th>Description</th>
                    <th>Counsellor</th>
                    <th>Prescription</th>
                </tr>
            </thead>
            <tbody id="supportRequests">
                {{#each supportRequests}}
                <tr>
                    <td>{{this.formattedDate}}</td>
                    <td>{{this.formattedTime}}</td>
                    <td>{{this.issue}}</td>
                    <td>{{this.description}}</td>
                    <td>{{this.counsellor}}</td>
                    <td>{{this.prescription}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</body>
</html>
